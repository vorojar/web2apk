<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 11+ 包可见性声明，用于检测和打开其他 APP -->
    <queries>
        <!-- 电商 -->
        <package android:name="com.taobao.taobao" />
        <package android:name="com.jingdong.app.mall" />
        <package android:name="com.xunmeng.pinduoduo" />
        <!-- 社交 -->
        <package android:name="com.tencent.mm" />
        <package android:name="com.xingin.xhs" />
        <package android:name="com.ss.android.ugc.aweme" />
        <!-- 支付 -->
        <package android:name="com.eg.android.AlipayGphone" />
        <!-- 地图 -->
        <package android:name="com.autonavi.minimap" />
        <package android:name="com.baidu.BaiduMap" />
        <!-- 外卖 -->
        <package android:name="com.sankuai.meituan" />
        <package android:name="com.ele.me" />
        <!-- 允许打开任意 URL Scheme -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="*" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 拍照权限（运行时请求） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 位置权限（运行时请求，网页需要时才弹窗） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 振动权限（无需运行时请求） -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 通知权限（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 精确闹钟权限（Android 12+） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 生物识别权限（无需运行时请求） -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- 录音权限（运行时请求） -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 前台服务权限（Android 9+） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 前台服务类型权限（Android 14+），使用 MEDIA_PLAYBACK 覆盖音乐/视频等场景 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WebAPK"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name="com.webapk.app.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode|smallestScreenSize|screenLayout"
            android:supportsPictureInPicture="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep Links / App Links -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="DEEP_LINK_HOST_PLACEHOLDER" />
                <data android:scheme="http" android:host="DEEP_LINK_HOST_PLACEHOLDER" />
            </intent-filter>
            <!-- 分享目标：接收其他APP分享的链接 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- 内置浏览器 -->
        <activity
            android:name="com.webapk.app.ExternalBrowserActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.WebAPK" />

        <!-- 扫码界面 -->
        <activity
            android:name="com.webapk.app.ScanActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- 图片预览 -->
        <activity
            android:name="com.webapk.app.ImagePreviewActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- 视频录制 -->
        <activity
            android:name="com.webapk.app.VideoRecordActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- FileProvider 用于拍照 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 通知广播接收器 -->
        <receiver android:name="com.webapk.app.NotificationReceiver" />

        <!-- 桌面小组件 -->
        <receiver
            android:name="com.webapk.app.WidgetProvider"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- 前台服务：通知栏常驻，进程保活（音乐播放、导航等场景） -->
        <service
            android:name="com.webapk.app.ForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>

</manifest>
